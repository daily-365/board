<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
   <mapper namespace="web.dao.face.BoardDAO">
    
    
    <!-- 목록 -->
    <select id="selectAllList" resultType="BoardDTO">
    select boardNo,boardTitle,boardContent,boardHit,boardDate
    from tbl_board       
    order by boardNo desc    
    </select>
    
    <!-- 상세보기 -->
    <select id="selectByBoardNo" resultType="BoardDTO">
    select boardNo,boardTitle,boardContent,boardHit,boardDate
    from tbl_board       
    where boardNo = #{boardNo}   
    </select>
    
    
    <!-- 등록 -->
    <insert id="writeBoard">
     insert into tbl_board (boardNo,boardTitle,boardContent)
    values (tbl_board_seq.nextVal,#{boardTitle},#{boardContent})
    </insert>
   
   
   <!-- 수정 -->
   <update id="modifyBoard">
   update tbl_board
   set boardTitle=#{boardTitle},
   boardContent=#{boardContent}
   where boardNo = #{boardNo}
   </update>
   
   
   <!-- 삭제 -->
   <delete id="deleteBoard">
    delete from tbl_board
   where boardNo = #{boardNo}
    </delete>
   
   <!-- 페이지 네이션 -->
   <select id="pageNation" resultType="web.dto.BoardDTO" parameterType="web.dto.Paging">
     select boardNo,boardTitle,boardContent,boardDate,boardHit
     from 
     (select boardNo,boardTitle,boardContent,boardDate,boardHit, 
        row_number() over (order by boardNo desc ) as rnum 
        from tbl_board 
          ) b
          where rnum between #{startNum} and #{endNum}
          order by boardNo desc
        
   </select>
   
   <select id="pageListCount" resultType="int">
   <![CDATA[
    select count(boardNo)
    from tbl_board
    where boardNo >0   
   ]]>
         
   </select>
   
   
   
   
   </mapper>